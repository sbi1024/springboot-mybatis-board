<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.example.springbootmybatisboard.board.dao.BoardDao">

    <select id="listBoard" parameterType="com.test.example.springbootmybatisboard.board.dto.BoardParamDto" resultType="com.test.example.springbootmybatisboard.board.dto.BoardDto">
        select b.board_id,
               b.user_seq,
               u.user_name,
               u.user_profile_image,
               b.title,
               b.content,
               b.reg_dt,
               b.read_count
        from board.board b,
             board.users u
        where b.user_seq = u.user_seq
        order by b.board_id desc
        limit #{limit} offset #{offset};
    </select>

    <select id="listBoardTotalCount">
        select count(*)
        from board.board b
    </select>

    <select id="listBoardSearchWord" parameterType="com.test.example.springbootmybatisboard.board.dto.BoardParamDto" resultType="com.test.example.springbootmybatisboard.board.dto.BoardDto">
        select b.board_id,
               b.user_seq,
               u.user_name,
               u.user_profile_image,
               b.title,
               b.content,
               b.reg_dt,
               b.read_count
        from board.board b,
             board.users u
        where b.user_seq = u.user_seq
         and b.title like concat('%',#{searchWord},'%')
        order by b.board_id desc
        limit #{limit} offset #{offset};
    </select>

    <select id="listBoardTotalCountSearchWord" parameterType="com.test.example.springbootmybatisboard.board.dto.BoardParamDto">
        select count(*)
        from board.board b
        where title like concat('%',#{searchWord},'%')
    </select>
</mapper>